%article.articles
  - @articles.each do |article|
    %a.article{ href: article.path }
      %strong= article.title
      %p
        = CGI.escapeHTML "#{article.body[0..140]}..."
        %br
        %small= article.created_at.strftime("%Y-%m-%d %H:%M:%S")
